{
  "name": "mneme",
  "version": "0.1.0",
  "private": true,
  "description": "MNEME — Memory that knows its role. Graph · Vectors · Vault · Secrets.",
  "scripts": {
    "dev": "pnpm --filter @mneme/web dev",
    "build": "pnpm --filter @mneme/web build",
    "preview": "pnpm --filter @mneme/web preview",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
